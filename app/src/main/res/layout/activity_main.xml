<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.avias.vlib.MainActivity">

    <Button
        android:id="@+id/btnVelos"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="Vélos"
        android:layout_marginTop="40dp"
        android:background="@android:color/darker_gray"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btnMaintenance"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="Maintenance"
        android:layout_marginTop="20dp"
        android:background="@android:color/darker_gray"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@+id/btnVelos"
        app:layout_constraintLeft_toLeftOf="@+id/btnVelos" />

    <Button
        android:id="@+id/btnStation"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="Stations et plots"
        android:layout_marginTop="20dp"
        android:background="@android:color/darker_gray"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        app:layout_constraintTop_toBottomOf="@+id/btnMaintenance"
        app:layout_constraintLeft_toLeftOf="@+id/btnMaintenance" />

</android.support.constraint.ConstraintLayout>